server:
  port: 3000 # 前端默认访问端口号为3000
  servlet:
    context-path: / # 前端默认访问的根路径
spring:
  application:
    name: gateway-service  # 程序名就是服务名，nacos中显示的名字
  cloud:
    nacos:
      server-addr: 124.221.70.206:8848 #注册中心地址